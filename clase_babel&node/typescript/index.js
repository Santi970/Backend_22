var listaArray = [2, 3, 5, 7];
listaArray
    .map(function (x) { return x * x; })
    .forEach(function (x) { return console.log(x); });
var generateRadom = function () { return Math.round(Math.random() * 225); };
